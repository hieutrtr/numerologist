{
  "name": "api",
  "prefix": "",
  "projectType": "application",
  "sourceRoot": "apps/api/src",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/api && source venv/bin/activate && uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
        "cwd": "."
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/api && source venv/bin/activate && pytest tests/",
        "cwd": "."
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/api && source venv/bin/activate && flake8 src/",
        "cwd": "."
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/api && source venv/bin/activate && mypy src/",
        "cwd": "."
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/api && source venv/bin/activate && black src/",
        "cwd": "."
      }
    }
  },
  "tags": ["type:app", "scope:api"]
}

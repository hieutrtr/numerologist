[pytest]
testpaths = tests
asyncio_mode = auto
addopts = 
    --cov=src
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    -v
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
